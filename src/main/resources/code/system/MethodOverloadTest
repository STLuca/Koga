parser host;

usables {
    core.Int;
    core.Boolean;
    core.If;
    core.Exit;
}

documents {
    util.SimpleAdministrator;
}

test.MethodOverloadTest {

    main() {
        Int x 2;
        Int y 7;
        x + 1 + y;
        Exit;
    }

}