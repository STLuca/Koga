parser host;

usables {
    core.Byte;
    core.Int;
    core.Reference;
    core.Usable;
    core.Exit;
    core.AdminRef;
}

documents {
    collection.ArrayList;
    util.SimpleAdministrator;
}

test.ArrayListTest {

    main() {
        AdminRef admin init();
        Reference<ArrayList> list new();
        Usable<Int> mc ();
        Int size 5;
        list init(mc size);
        Int index 3;
        Int value 99;
        list set(index value);
        Exit;
    }

}