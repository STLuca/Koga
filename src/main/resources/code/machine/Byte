parser machineComposite;

core.Byte {
                
    Byte[1] val;

    constructor(b8 imm) {
        i(ADD, II, LDA, val, I, 0d0, L, imm);
    }

    plus(b8 imm) {
        i(ADD, TI, val, val, imm);
    }

}