parser host;

usables {
    core.Int;
    core.Boolean;
    core.While;
    core.Exit;
}

documents {
    util.SimpleAdministrator;
}

test.WhileTest {

    main() {
        Int x 0;
        Boolean y true;

        While (y) {
            x + 1;
            y = { x < 10; };
        };
        Exit;
    }

}