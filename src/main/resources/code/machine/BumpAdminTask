parser machineComposite;

core.BumpAdminTask {

    Byte[4] taskAddr;
    Byte[4] object;
    Byte[4] table;
    Byte[4] instruction;
    Byte[4] altTask;
    Byte[4] return;

    constructor loadInAdmin() {
        i(ADD, LI, LDA, taskAddr, R, altTask, I, 0d0);
        i(ADD, LI, LDA, object, R, object, I, 0d0);
        i(ADD, LI, LDA, table, R, table, I, 0d0);
        i(ADD, LI, LDA, instruction, R, instruction, I, 0d0);
        i(ADD, LI, LDA, altTask, R, task, I, 0d0);
    }

}