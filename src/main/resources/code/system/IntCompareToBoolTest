parser system;

imports {
    Byte;
    Int;
    Boolean;
    Exit;
    AdminRef;
}

dependencies {
    BumpAdministrator;
}

IntCompareToBoolTest {

    main() {
        Int x 10;
        Boolean y false;
        x < (15 y);
        Exit;
    }

}