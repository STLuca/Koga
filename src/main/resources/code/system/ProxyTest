parser host;

structures {
    core.Byte;
    core.Int;
    core.Optional;
    core.Proxy;
    core.Exit;
}

documents {
    util.SimpleAdministrator;
}

test.ProxyTest {

    main() {
        Int x 1;
        Optional<Int> y (x);
        Proxy<Int> z;
        y get(z);
        z + 2;
        Exit;
    }

}