parser host;

usables {
    core.Byte;
    core.Int;
    core.Try;
    core.Exit;
}

documents {
    util.SimpleAdministrator;
}

test.ExceptionTest {

    main() {
        Int x 0;
        Try t {
            t throw();
            x plus(5);
        } handle {
            x plus(10);
        };
        Exit;
    }

}