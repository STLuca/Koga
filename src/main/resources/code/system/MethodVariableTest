parser host;

imports {
    Byte;
    Int;
    Exit;
}

dependencies {
    util.SimpleAdministrator;
}

test.MethodVariableTest {

    main() {
        Int x 4;
        x plusTwice(5);
        Exit;
    }

}