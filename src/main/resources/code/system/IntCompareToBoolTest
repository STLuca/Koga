parser host;

imports {
    Byte;
    Int;
    Boolean;
    Exit;
    AdminRef;
}

dependencies {
    util.SimpleAdministrator;
}

test.IntCompareToBoolTest {

    main() {
        Int x 10;
        Boolean y false;
        x < (15 y);
        Exit;
    }

}