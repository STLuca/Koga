parser machine;

core.Task {

    constructor complete() {
        Admin(TRANSITION);
    }

    constructor fail() {
        Byte[4] status;
        i(ADD, II, LDA, status, IL, 0d1, IL, 0d0);
        Admin(TRANSITION, status);
    }

}