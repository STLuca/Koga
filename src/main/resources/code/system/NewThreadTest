parser host;

usables {
    core.Int;
    core.Thread;
    core.Exit;
}

documents {
    util.SimpleAdministrator;
}

test.NewThreadTest {

    main() {
        AdminRef admin init();
        Thread<NewThreadTest> this this();
        Int x 13;
        this anything(x);
        Exit;
    }

    anything(Int x) {
        Exit;
    }


}