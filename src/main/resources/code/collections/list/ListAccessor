parser interface;

structures {
    core.Int;
    core.Pointer;
}

collection.ListAccessor<Structure T> {

    get(Pointer<T> structOut);
    size(Pointer<Int> intOut);
    add(Pointer<T> struct);
    remove();
    next();
    previous();

}
