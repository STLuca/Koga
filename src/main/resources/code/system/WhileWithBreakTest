parser host;

structures {
    core.Byte;
    core.Int;
    core.If;
    core.While;
    core.Exit;
    core.Boolean;
}

documents {
    util.SimpleAdministrator;
}

test.WhileWithBreakTest {

    main() {
        Int x 0;
        Boolean y true;

        While a (y) {
            If(x) {
                a break();
            };
            x + 1;
            y = { x < 10; };
        };
        Exit;
    }

}