parser host;

imports {
    Byte;
    Int;
    Optional;
    Proxy;
    Exit;
}

dependencies {
    util.SimpleAdministrator;
}

test.ProxyTest {

    main() {
        Int x 1;
        Optional<Int> y (x);
        Proxy<Int> z;
        y get(z);
        z + 2;
        Exit;
    }

}