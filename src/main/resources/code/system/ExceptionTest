parser system;

imports {
    Byte;
    Int;
    Try;
    Exit;
}

dependencies {
    BumpAdministrator;
}

ExceptionTest {

    main() {
        Int x 0;
        Try t {
            t throw();
            x plus(5);
        } handle {
            x plus(10);
        };
        Exit;
    }

}