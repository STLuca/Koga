parser host;

structures {
    core.Byte;
    core.Int;
    core.Boolean;
    core.If;
    core.While;
    core.LocalDate;
    core.Exit;
}

documents {
    util.SimpleAdministrator;
}

test.StructureTest {

    main() {
        LocalDate date;
        Int addDays 30;
        date addDays(addDays);
        date testing(addDays) {
            addDays + 5;
        };
        LocalDate today today();
        Exit;
    }

}