parser host;

structures {
    core.Byte;
    core.Int;
    core.Reference;
    core.Structure;
    core.Exit;
    core.AdminRef;
}

documents {
    collection.ArrayList;
    util.SimpleAdministrator;
}

test.ArrayListTest {

    main() {
        AdminRef admin init();
        Reference<ArrayList> list new();
        Structure<Int> mc ();
        Int size 5;
        list init(mc size);
        Int index 3;
        Int value 99;
        list set(index value);
        Exit;
    }

}