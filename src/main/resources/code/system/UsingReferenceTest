parser host;

structures {
    core.Byte;
    core.Int;
    core.Reference;
    core.Pointer;
    core.Exit;
    core.AdminRef;
}

documents {
    test.Fields;
    util.SimpleAdministrator;
}

test.UsingReferenceTest {

    Reference<Fields> f;

    main() {
        AdminRef admin init();
        Int x 99;
        Reference<UsingReferenceTest> this this();
        Reference<Fields> new new();
        Pointer<Reference> thisF (this f);
        thisF <- new;
        new addInput(x);
        Exit;
    }

}
