parser system;

imports {
    Byte;
    Int;
    Optional;
    Proxy;
    Exit;
}

dependencies {
    BumpAdministrator;
}

ProxyTest {

    main() {
        Int x 1;
        Optional<Int> y (x);
        Proxy<Int> z;
        y get(z);
        z + 2;
        Exit;
    }

}