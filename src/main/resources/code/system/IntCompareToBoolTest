parser host;

usables {
    core.Byte;
    core.Int;
    core.Boolean;
    core.Exit;
    core.AdminRef;
}

documents {
    util.SimpleAdministrator;
}

test.IntCompareToBoolTest {

    main() {
        Int x 10;
        Boolean y false;
        x < (15 y);
        Exit;
    }

}