parser host;

usables {
    core.Byte;
    core.Int;
    core.Exit;
}

documents {
    util.SimpleAdministrator;
}

test.MethodVariableTest {

    main() {
        Int x 4;
        x plusTwice(5);
        Exit;
    }

}