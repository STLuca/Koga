parser machineComposite;

core.BumpAdminTask {

    Byte[4] taskAddr;
    Byte[4] object;
    Byte[4] table;
    Byte[4] instruction;
    Byte[4] altTask;
    Byte[4] return;

    constructor loadInAdmin() {
        i(ADD, LI, P, taskAddr, R, altTask, I, 0d0);
        i(ADD, LI, P, object, R, object, I, 0d0);
        i(ADD, LI, P, table, R, table, I, 0d0);
        i(ADD, LI, P, instruction, R, instruction, I, 0d0);
        i(ADD, LI, P, altTask, R, task, I, 0d0);
    }

}