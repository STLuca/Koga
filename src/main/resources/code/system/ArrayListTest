parser system;

imports {
    Byte;
    Int;
    Reference;
    MachineClass;
    Exit;
    AdminRef;
}

dependencies {
    ArrayList;
    BumpAdministrator;
}

ArrayListTest {

    main() {
        AdminRef admin init();
        Reference<ArrayList> list new();
        MachineClass<Int> mc ();
        Int size 5;
        list init(mc size);
        Int index 3;
        Int value 99;
        list set(index value);
        Exit e ();
    }

}