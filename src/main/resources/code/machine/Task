parser machineComposite;

core.Task {

    constructor complete() {
        Admin(TRANSITION);
    }

    constructor fail() {
        Byte[4] status;
        i(ADD, II, P, status, I, 0d1, I, 0d0);
        Admin(TRANSITION, status);
    }

}