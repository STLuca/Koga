parser host;

imports {
    Byte;
    Int;
    Exit;
}

dependencies {
    util.SimpleAdministrator;
}

test.NewTest {

    main() {
        Int x 4;
        x makeBlock {
            x usingContext();
        };
        Exit;
    }

}