parser host;

imports {
    Byte;
    Int;
    Pointer;
    Exit;
}

dependencies {
    SimpleAdministrator;
}

PointerTest {

    main() {
        Int x 1;
        Int y 2;
        Int z 3;
        Pointer<Int> p (x);
        p -> y;
        p <- z;
        Exit;
    }

}