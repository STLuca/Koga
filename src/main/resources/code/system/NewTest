parser host;

usables {
    core.Byte;
    core.Int;
    core.Exit;
}

documents {
    util.SimpleAdministrator;
}

test.NewTest {

    main() {
        Int x 4;
        x makeBlock {
            x usingContext();
        };
        Exit;
    }

}