parser machineComposite;

core.Deferrer {

    Addr end;

    constructor() {
        Direction(after);
        Addr end;
        Direction(before);
    }

    constructor(Block b) {
        Direction(after);
        Addr end;
        Direction(before);
        Block b;
        Position end;
    }

    defer(Block b) {
        Position end prev;
        Block b;
        Position prev;
    }

    close() {
        Position end;
    }

}