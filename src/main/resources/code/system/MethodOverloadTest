parser host;

imports {
    Int;
    Boolean;
    If;
    Exit;
}

dependencies {
    util.SimpleAdministrator;
}

test.MethodOverloadTest {

    main() {
        Int x 2;
        Int y 7;
        x + 1 + y;
        Exit;
    }

}